# Audio Viewer + effect-streaming runtime.
# Installs cleanly with: pip install -r requirements.txt
# (no GitHub-only packages — research/beat-validation extras are split out into
#  requirements-research.txt so a fresh viewer setup never hits the madmom build.)
sounddevice>=0.4.6
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0
librosa>=0.10.0
matplotlib>=3.7.0
pyserial>=3.5
pyyaml>=6.0
tqdm>=4.67.0
